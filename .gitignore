node_modules/
dist/

# Fetched or copied by `npm run assets` (see scripts/fetch-assets.mjs).
# ~26 MB of wasm and model weights — reproducible, so not worth committing.
public/mediapipe/
public/models/

# Rendered by the dev scripts in scripts/visual/ for eyeballing a change.
# Regenerate with `npm run parts`, `npm run puppet`, `npm run arms`, etc.
/*.png

*.log
.DS_Store
